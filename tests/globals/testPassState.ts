import PassState from '../../src/state/passState/PassState';

const testPassState: PassState = new PassState(
  'Test Pass State', undefined, { result: 'end' }, 'Test Pass State',
);

export default testPassState;
